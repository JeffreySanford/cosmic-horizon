#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# run our lint suite (includes markdown, mermaid and docs coverage)
pnpm run lint || {
  echo "Pre-push lint failed, aborting push." >&2
  exit 1
}

# optionally you can add additional checks here
